<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <title>CRUD App</title>
  </head>
  <body>
    <div class="hero">
      <div>
        <h1>May Node and Express be with you.</h1>

        <form action="/quotes" method="post">
          <!-- Method tells the browser what kind of request to send, we use POST because we’re sending a POST request.
  
          The action attribute tells the browser where to send the POST request. In this case, we’re send the POST request to /quotes. 
          -->
          <input type="text" placeholder="Name:" name="name" />
          <input type="text" placeholder="Quote:" name="quote" />
          <button type="submit">Submit</button>
        </form>
      </div>
      <div>
        <h2>Darth Vadar invades!</h2>
        <p>Replace first Ahmed's quote with a quote written by Darth Vadar</p>
        <button id="update-button">Replace Ahmed's quote</button>
      </div>
      <div>
        <h2>Remove Luke's Quote!</h2>
        <p>
          Delete one Luke Skywalker quote. Does nothing if there are no more
          Luke quote
        </p>
        <button id="delete-button">Delete Luke's quote</button>
        <div id="message"></div>
      </div>
    </div>

    <div class="quotes">
      <h2>Quotes</h2>

      <ul class="quotes">
        <!-- Loop through quotes -->
        <% for(var i = 0; i < quotes.length; i++) {%>
        <li class="quote">
          <!-- Output name from the iterated quote object -->
          <span><%= quotes[i].name %></span>:
          <!-- Output quote from the iterated quote object -->
          <span><%= quotes[i].quote %></span>
        </li>
        <% } %>
      </ul>
    </div>
    <script src="/main.js"></script>
  </body>
</html>
